import localFont from "next/font/local";

export const outrun = localFont({
  src: "./Outrun.ttf",
  variable: "--font-outrun",
  display: "swap",
});

export const meshedDisplay = localFont({
  src: "./MeshedDisplay-Regular.otf",
  variable: "--font-meshed",
  display: "swap",
});